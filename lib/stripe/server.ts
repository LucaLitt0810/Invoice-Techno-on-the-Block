// Placeholder Stripe server module
export const stripe = null;

// Placeholder function for checkout session
export async function createCheckoutSession(data: any) {
  throw new Error('Stripe not configured');
}
